h1 = t('category.categories')

.categories
  - @categories.each do |category|
    .category-item
      = render category
      p.text-left
        = link_to t('category.show'), category, class: 'btn btn-primary btn-lg'
